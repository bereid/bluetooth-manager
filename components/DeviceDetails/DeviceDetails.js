import React from 'react';
import { SafeAreaView, ScrollView, View, Text } from 'react-native';

const DeviceDetails = () => {
  return (
    <>
    <SafeAreaView>
        <ScrollView>
          <Text>DeviceDetails</Text>
        </ScrollView>
      </SafeAreaView>
    </>
  );
};


export default DeviceDetails;
